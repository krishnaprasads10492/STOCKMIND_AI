# StockMind AI — Python ML Backend
# ALL versions are pinned for security and reproducibility.
# Last audited: June 2026 | Min Python: 3.10 (3.11/3.12 recommended for Windows)
#
# IMPORTANT — NUMPY ON WINDOWS:
#   numpy 1.26.4 = last version without Meson build (Python ≤3.12 only)
#   numpy 2.2.6  = required for Python ≥3.13 (Meson build, needs Windows Long Paths)
#   The launcher (start.js) auto-detects your Python version and installs the
#   correct numpy using C:\Tmp as TEMP directory (short path) to avoid failures.
#   This file uses numpy>=1.26.4 so pip resolves correctly for each Python version.

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3
pydantic==2.11.7

# ── Data ─────────────────────────────────────────────────────────────────────
# numpy: flexible pin — launcher installs version-specific wheel before this runs
numpy>=1.26.4
pandas==2.2.3
scipy==1.14.1

# ── ML — Ensemble models ──────────────────────────────────────────────────────
scikit-learn==1.5.2
lightgbm==4.5.0
xgboost==2.1.3

# ── Time-series ───────────────────────────────────────────────────────────────
statsmodels==0.14.4

# ── Finance math ──────────────────────────────────────────────────────────────
numpy-financial==1.0.0

# ── Technical indicators ──────────────────────────────────────────────────────
ta==0.11.0

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.28.1

# ── Image analysis ────────────────────────────────────────────────────────────
Pillow==11.2.1
pytesseract==0.3.13

# ── Document parsing (Excel upload, doc-intel) ───────────────────────────────
openpyxl==3.1.5

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.1.0
joblib==1.4.2

# ── Deep learning (optional — uncomment if GPU available) ────────────────────
# torch==2.7.0
# tensorflow==2.19.0

# ── Sentiment (optional — downloads ~500MB model on first run) ────────────────
# transformers==4.51.3
# torch==2.7.0

# ── Advanced ML (optional — production training only) ────────────────────────
# catboost==1.2.7
# optuna==4.3.0
# shap==0.47.2
