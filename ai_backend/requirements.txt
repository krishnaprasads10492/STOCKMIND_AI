# StockMind AI — Python ML Backend
# ALL versions are pinned for security and reproducibility.
# Last audited: July 2026 | Min Python: 3.10 | Python 3.14 compatible
#
# IMPORTANT — NUMPY ON WINDOWS / PYTHON 3.14:
#   numpy 1.26.4 = last version without Meson build (Python ≤3.12 only)
#   numpy 2.x    = required for Python ≥3.13 (pre-built wheels available)
#   pandas 3.x   = required for Python ≥3.13 (pre-built wheels available)
#   This file uses flexible lower-bounds so pip resolves the best available wheel.
#   Pinned ranges keep builds reproducible while allowing Python 3.14 wheels.

# ── Core API ──────────────────────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.3
# pydantic 2.11.x needs pydantic-core==2.33.x which has no Python 3.14 wheel
# pydantic 2.13+ ships Python 3.14 compatible pydantic-core wheels
pydantic>=2.11.7

# ── Data ─────────────────────────────────────────────────────────────────────
# numpy: 2.x required for Python 3.13+; 1.26.x fine for 3.10–3.12
numpy>=1.26.4
# pandas: 2.2.x has no Python 3.13/3.14 wheels — 3.0+ does
pandas>=2.2.3
scipy>=1.14.1

# ── ML — Ensemble models ──────────────────────────────────────────────────────
scikit-learn>=1.5.2
lightgbm>=4.5.0
xgboost>=2.1.3

# ── Time-series ───────────────────────────────────────────────────────────────
# statsmodels 0.14.x has no Python 3.14 wheels — use latest compatible
statsmodels>=0.14.4

# ── Finance math ──────────────────────────────────────────────────────────────
numpy-financial==1.0.0

# ── Technical indicators ──────────────────────────────────────────────────────
ta==0.11.0

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx==0.28.1

# ── Image analysis ────────────────────────────────────────────────────────────
Pillow>=11.2.1
pytesseract==0.3.13

# ── Document parsing (Excel upload, doc-intel) ───────────────────────────────
openpyxl==3.1.5

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.1.0
joblib>=1.4.2

# ── Deep learning (optional — uncomment if GPU available) ────────────────────
# torch==2.7.0
# tensorflow==2.19.0

# ── Sentiment (optional — downloads ~500MB model on first run) ────────────────
# transformers==4.51.3
# torch==2.7.0

# ── Advanced ML (optional — production training only) ────────────────────────
# catboost==1.2.7
# optuna==4.3.0
# shap==0.47.2
