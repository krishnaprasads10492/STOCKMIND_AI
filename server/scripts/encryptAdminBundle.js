/**
 * encryptAdminBundle.js — DISABLED as a standalone CLI script.
 *
 * The embedded admin bundle is regenerated exclusively through the
 * application UI (Settings → Security → Rebuild Bundle).
 * That endpoint requires super-admin authentication + passphrase verification
 * before it will write a new bundle.
 *
 * Running this script directly does nothing.
 */
console.error('This script is disabled. Rebuild the bundle through the app UI only.')
process.exit(1)
