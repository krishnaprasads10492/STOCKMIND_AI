/**
 * setupSuperAdmin.js — DISABLED
 *
 * The admin vault is managed exclusively through the application UI.
 * To update vault credentials:
 *   1. Log in as super-admin
 *   2. Go to Settings → Security → Update Vault
 *   3. Enter your current passphrase to authenticate
 *   4. Enter new credentials + new passphrase
 *
 * This script no longer functions and cannot be used to overwrite the vault.
 */
console.error('This script is disabled. Manage the vault through the app UI only.')
process.exit(1)
